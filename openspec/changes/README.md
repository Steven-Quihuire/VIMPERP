# Active Changes

Each directory here is one independent SDD change. Keep the change small enough to explore, implement, verify, and archive without carrying unrelated feature work.

Use a new directory for every follow-up. If a previous change is incomplete, leave its history intact and create a dependent change rather than rewriting its scope after implementation.

Completed changes belong under `archive/` only after all persisted tasks are checked and verification has passed.
